import org.amcworld.springcrm.Organization

model {
    List<Organization> organizationList
}

json(organizationList ?: Collections.EMPTY_LIST) { Organization org ->
    id org.id.toString()
    name org.name
}
